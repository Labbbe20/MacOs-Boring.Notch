name: Crowdin Sync

on:
  workflow_dispatch:

jobs:
  crowdin:
    runs-on: ubuntu-latest
    steps:
      - name: Crowdin sync is not configured
        run: echo "Crowdin sync is disabled for this fork."
